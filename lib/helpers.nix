# lib/helpers.nix
#
# Shared helper functions for the pugs-tf monorepo.
#
# Planned helpers:
#   - fetchSourceModPlugin: download and compile a SourceMod plugin
#     from source (.sp -> .smx) using the spcomp compiler
#   - mkServerConfig: assemble a TF2 server config directory from
#     individual cfg fragments
#   - mkPluginPack: bundle a set of compiled plugins + gamedata + translations
{ pkgs }:

{
  # Helper implementations will be added as needed.
}
